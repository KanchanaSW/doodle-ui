[build]
  command = "pnpm --filter doodleui-react build && pnpm --filter docs build"
  publish = "apps/docs/out"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "9.15.9"
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
