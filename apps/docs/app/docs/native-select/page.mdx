import { ComponentPlayground } from "@/components/ComponentPlayground";
import { PropsTable } from "@/components/Playground";
import { nativeSelectProps } from "@/lib/props";
import { ComponentCliInstallTabs } from "@/components/InstallCommands";

# Native Select

A native `<select>` with a sketch border and hand-drawn chevron — no custom popover.

<ComponentPlayground name="native-select" />

## CLI install

<ComponentCliInstallTabs name="native-select" />

## Usage

```tsx
import { NativeSelect } from "doodleui-react";

<NativeSelect
  defaultValue="draft"
  options={[
    { value: "draft", label: "Draft" },
    { value: "done", label: "Done" },
  ]}
/>
```

## Props

<PropsTable rows={nativeSelectProps} />
