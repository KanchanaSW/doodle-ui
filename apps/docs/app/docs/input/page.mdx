import { ComponentPlayground } from "@/components/ComponentPlayground";
import { PropsTable } from "@/components/Playground";
import { inputProps } from "@/lib/props";
import { ComponentCliInstallTabs } from "@/components/InstallCommands";

# Input

Single-line text field with a sketchy border. Native focus rings are removed. Focus re-inks the border with a second seed (and still shifts the stroke toward the accent color).

<ComponentPlayground name="input" />

## CLI install

<ComponentCliInstallTabs name="input" />

## Usage

```tsx
import { Input } from "doodleui-react";

<Input label="Email" placeholder="you@studio.dev" />
```

Pass a `label` instead of using placeholder-as-label. All native input attributes (`type`, `name`, `autoComplete`, `onChange`) forward to the control.

## Props

<PropsTable rows={inputProps} />
